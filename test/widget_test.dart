// `flutter create` auto-generates this file with a counter-app smoke test
// that doesn't apply to EQ Cards. Real tests live in `test/core/` and
// `test/features/`. Keeping a single trivially-passing test here so the
// `flutter test` command still has a default entry point.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — real tests live in test/core/ and test/features/', () {
    expect(true, isTrue);
  });
}
